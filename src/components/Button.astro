---
const {type = 'button', id} = Astro.props;
---

<style lang="scss">
button {
  padding: 1rem 2rem;
  text-align: center;
  font-family: var(--font-family-poppins);
  font-weight: 500;
  font-size: 1rem;

  background-color: #fff;
  border: 2px solid var(--clr-primary);
  box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1), 0px 8px 16px rgba(0, 0, 0, 0.15);
  border-radius: 8px;
}
</style>


<button type={type} id={id}>
  <slot></slot>
</button>
